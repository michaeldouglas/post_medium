# Instação

```bash
uv add fastapi uvicorn requests langchain openai pydantic fastmcp langchain-community mcp langchain-openai langgraph python-dotenv langchain-mcp-adapters
```

## Subir API

```bash
uvicorn api:app --port 9000 --reload
```
